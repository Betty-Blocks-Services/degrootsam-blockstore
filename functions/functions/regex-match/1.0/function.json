{
  "category": "Regex",
  "description": "Matches the provided value against a regex and returns all matches",
  "icon": {
    "color": "Orange",
    "name": "MagnifyingGlassIcon"
  },
  "label": "Regex Match",
  "options": [
    {
      "name": "regex",
      "label": "Regex",
      "meta": {
        "type": "MultilineText",
        "default": "([A-Z])\\w+",
        "placeholder": "([A-Z])\\w+",
        "validations": { "required": true }
      },
      "info": "The regular expression pattern to match against the value."
    },
    {
      "name": "flagCaseInsensitive",
      "label": "Case insensitive",
      "meta": {
        "type": "Boolean",
        "default": false
      },
      "info": "Adds the 'i' flag. Ignores letter case when matching."
    },
    {
      "name": "flagMultiline",
      "label": "Multiline",
      "meta": {
        "type": "Boolean",
        "default": false
      },
      "info": "Adds the 'm' flag. Makes '^' and '$' match the start/end of each line instead of the whole string."
    },
    {
      "name": "flagSingleLine",
      "label": "Single line (dotall)",
      "meta": {
        "type": "Boolean",
        "default": false
      },
      "info": "Adds the 's' flag. Makes '.' match newline characters as well."
    },
    {
      "name": "flagUnicode",
      "label": "Unicode",
      "meta": {
        "type": "Boolean",
        "default": false
      },
      "info": "Adds the 'u' flag. Treats the pattern as a sequence of Unicode code points."
    },
    {
      "name": "flagSticky",
      "label": "Sticky",
      "meta": {
        "type": "Boolean",
        "default": false
      },
      "info": "Adds the 'y' flag. Matches only from the position given by the regex's lastIndex, so subsequent matches must be contiguous."
    },
    {
      "name": "value",
      "label": "Value to match",
      "meta": {
        "type": "Value",
        "allowedKinds": ["STRING", "INTEGER"],
        "validations": {
          "required": true
        }
      },
      "info": "The value to match using the regex."
    },
    {
      "name": "result",
      "label": "Result",
      "meta": {
        "type": "Output",
        "output": {
          "type": "Array",
          "dataType": "STRING"
        },
        "validations": {
          "required": true
        }
      },
      "info": "An array of all matches found in the value."
    }
  ],
  "yields": "NONE"
}
