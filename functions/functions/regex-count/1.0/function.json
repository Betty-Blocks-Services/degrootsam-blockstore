{
  "category": "Regex",
  "description": "Counts the number of times a regex matches the provided value",
  "icon": {
    "color": "Orange",
    "name": "ActionsIcon"
  },
  "label": "Regex Count",
  "options": [
    {
      "name": "regex",
      "label": "Regex",
      "meta": {
        "type": "MultilineText",
        "default": "([A-Z])\\w+",
        "placeholder": "([A-Z])\\w+",
        "validations": { "required": true }
      },
      "info": "The regular expression pattern to count matches of."
    },
    {
      "name": "flagCaseInsensitive",
      "label": "Case insensitive",
      "meta": {
        "type": "Boolean",
        "default": true
      },
      "info": "Adds the 'i' flag. Ignores letter case when matching."
    },
    {
      "name": "flagMultiline",
      "label": "Multiline",
      "meta": {
        "type": "Boolean",
        "default": true
      },
      "info": "Adds the 'm' flag. Makes '^' and '$' match the start/end of each line instead of the whole string."
    },
    {
      "name": "flagSingleLine",
      "label": "Single line (dotall)",
      "meta": {
        "type": "Boolean",
        "default": true
      },
      "info": "Adds the 's' flag. Makes '.' match newline characters as well."
    },
    {
      "name": "flagUnicode",
      "label": "Unicode",
      "meta": {
        "type": "Boolean",
        "default": true
      },
      "info": "Adds the 'u' flag. Treats the pattern as a sequence of Unicode code points."
    },
    {
      "name": "flagSticky",
      "label": "Sticky",
      "meta": {
        "type": "Boolean",
        "default": true
      },
      "info": "Adds the 'y' flag. Matches only from the position given by the regex's lastIndex, so subsequent matches must be contiguous."
    },
    {
      "name": "value",
      "label": "Value to test",
      "meta": {
        "type": "Value",
        "allowedKinds": ["STRING", "INTEGER"],
        "validations": {
          "required": true
        }
      },
      "info": "The value to count matches in."
    },
    {
      "name": "result",
      "label": "Output",
      "meta": {
        "type": "Output",
        "output": { "type": "Number" },
        "validations": { "required": true }
      },
      "info": "The number of matches found."
    }
  ],
  "yields": "NONE"
}
